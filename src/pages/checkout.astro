---
import Layout from "../layouts/Layout.astro";
---

<Layout
    title="Request Your Tour | Vamos Jacó Tours"
    description="Request your tour booking with Vamos Jacó Tours. We will confirm via WhatsApp and you can pay on arrival."
    robots="noindex, nofollow"
>
    <div class="min-h-screen bg-dark pt-32 pb-20">
        <div class="max-w-4xl mx-auto px-6">
            <h1
                class="text-4xl md:text-5xl font-black text-white mb-2 border-b border-white/10 pb-6"
                data-i18n="h1Title"
            >
                Request Your Tour
            </h1>
            <p class="text-gray-400 mb-8 -mt-4" data-i18n="requestSubtitle">
                No payment required today. Reserve your spot, we confirm via WhatsApp, and you can pay on arrival.
            </p>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Left: Booking Summary -->
                <div class="md:col-span-2 space-y-8">
                    <!-- Guest Details Form -->
                    <div
                        class="bg-dark-soft rounded-2xl border border-white/10 p-6 md:p-8"
                    >
                        <h2
                            class="text-xl font-bold text-white mb-6 flex items-center gap-3"
                        >
                            <span
                                class="w-8 h-8 rounded-full bg-primary/20 text-primary flex items-center justify-center text-sm font-bold"
                                >1</span
                            >
                            Your Details
                        </h2>
                        <form id="checkout-form" class="space-y-4" novalidate>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div>
                                    <label
                                        class="block text-sm text-gray-400 mb-1"
                                        >First Name <span class="text-primary">*</span></label
                                    >
                                    <input
                                        id="input-firstName"
                                        type="text"
                                        required
                                        class="w-full bg-dark border border-white/10 rounded-lg px-4 py-3 text-white focus:border-primary focus:outline-none transition-colors"
                                        placeholder="John"
                                    />
                                    <p class="text-primary text-xs mt-1 hidden" id="error-firstName">Please enter your first name</p>
                                </div>
                                <div>
                                    <label
                                        class="block text-sm text-gray-400 mb-1"
                                        >Last Name <span class="text-primary">*</span></label
                                    >
                                    <input
                                        id="input-lastName"
                                        type="text"
                                        required
                                        class="w-full bg-dark border border-white/10 rounded-lg px-4 py-3 text-white focus:border-primary focus:outline-none transition-colors"
                                        placeholder="Doe"
                                    />
                                    <p class="text-primary text-xs mt-1 hidden" id="error-lastName">Please enter your last name</p>
                                </div>
                            </div>
                            <div>
                                <label class="block text-sm text-gray-400 mb-1"
                                    >Email Address <span class="text-primary">*</span></label
                                >
                                <input
                                    id="input-email"
                                    type="email"
                                    required
                                    class="w-full bg-dark border border-white/10 rounded-lg px-4 py-3 text-white focus:border-primary focus:outline-none transition-colors"
                                    placeholder="john@example.com"
                                />
                                <p class="text-primary text-xs mt-1 hidden" id="error-email">Please enter a valid email address</p>
                            </div>
                            <div>
                                <label class="block text-sm text-gray-400 mb-1"
                                    >Phone Number (WhatsApp) <span class="text-primary">*</span></label
                                >
                                <div class="flex gap-2">
                                    <div class="relative shrink-0" id="country-selector">
                                        <button
                                            type="button"
                                            id="country-btn"
                                            class="bg-dark border border-white/10 rounded-lg px-3 py-3 text-white flex items-center gap-2 hover:border-primary/50 transition-colors min-w-[110px] whitespace-nowrap"
                                        >
                                            <span id="country-flag">🇺🇸</span>
                                            <span id="country-code" class="text-sm font-medium">+1</span>
                                            <svg class="w-3 h-3 text-gray-500 ml-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                                        </button>
                                        <div id="country-dropdown" class="hidden absolute bottom-full left-0 mb-2 w-72 max-h-64 overflow-y-auto bg-[#1A1A1A] border border-white/10 rounded-xl shadow-2xl z-50">
                                            <div class="p-2 sticky top-0 bg-[#1A1A1A]">
                                                <input type="text" id="country-search" placeholder="Search country..." class="w-full bg-white/5 border border-white/10 rounded-lg px-3 py-2 text-sm text-white placeholder-gray-500 outline-none focus:border-primary"/>
                                            </div>
                                            <div id="country-list" class="p-1"></div>
                                        </div>
                                    </div>
                                    <input
                                        id="input-phone"
                                        type="tel"
                                        required
                                        class="flex-1 min-w-0 bg-dark border border-white/10 rounded-lg px-4 py-3 text-white focus:border-primary focus:outline-none transition-colors"
                                        placeholder="555 000 0000"
                                    />
                                </div>
                                <input type="hidden" id="input-full-phone" />
                                <p class="text-primary text-xs mt-1 hidden" id="error-phone">Please enter your phone number</p>
                            </div>
                        </form>
                    </div>

                    <!-- Payment Info (no online payment) -->
                    <div
                        class="bg-dark-soft rounded-2xl border border-white/10 p-6 md:p-8"
                    >
                        <h2
                            class="text-xl font-bold text-white mb-6 flex items-center gap-3"
                        >
                            <span
                                class="w-8 h-8 rounded-full bg-primary/20 text-primary flex items-center justify-center text-sm font-bold"
                                >2</span
                            >
                            Payment
                        </h2>

                        <div class="space-y-4">
                            <div
                                class="bg-green-500/10 border border-green-500/20 rounded-xl p-4 flex items-start gap-3"
                            >
                                <svg class="w-5 h-5 text-green-400 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
                                <div>
                                    <h4 class="text-white font-bold" data-i18n="noPayNowTitle">No payment now</h4>
                                    <p class="text-xs text-gray-400 mt-1" data-i18n="payOnArrivalText">
                                        You can pay when you arrive at the tour (cash or card), or ask us on WhatsApp for a secure payment link.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Right: Order Summary -->
                <div class="md:col-span-1">
                    <div
                        class="bg-dark-soft rounded-2xl border border-white/10 p-6 sticky top-28"
                    >
                        <h3 class="text-lg font-bold text-white mb-4">
                            Order Summary
                        </h3>

                        <!-- Dynamic Content Placeholder (Client-side populated) -->
                        <div id="order-summary-content" class="space-y-4 mb-6">
                            <div
                                class="skeleton h-4 w-3/4 bg-white/10 rounded animate-pulse"
                            >
                            </div>
                            <div
                                class="skeleton h-4 w-1/2 bg-white/10 rounded animate-pulse"
                            >
                            </div>
                        </div>

                        <div class="border-t border-white/10 pt-4 mb-6">
                            <div class="flex justify-between items-center mb-2">
                                <span class="text-gray-400">Subtotal</span>
                                <span
                                    class="text-white font-medium"
                                    id="summary-subtotal">--</span
                                >
                            </div>
                            <div class="flex justify-between items-center mb-2">
                                <span class="text-gray-400">Taxes</span>
                                <span class="text-white font-medium">$0.00</span
                                >
                            </div>
                            <div
                                class="flex justify-between items-center pt-2 border-t border-white/10 mt-2"
                            >
                                <span class="text-white font-bold text-lg"
                                    >Total</span
                                >
                                <span
                                    class="text-brand-orange font-black text-2xl"
                                    id="summary-total">--</span
                                >
                            </div>
                            <p class="text-xs text-gray-500 mt-3" data-i18n="dueOnArrival">
                                Total due on arrival (no online payment).
                            </p>
                        </div>

                        <button
                            id="pay-button-desktop"
                            class="hidden md:flex w-full bg-primary hover:bg-primary/90 text-white font-bold py-4 rounded-xl shadow-lg shadow-primary/20 transition-all active:scale-95 items-center justify-center gap-2"
                            data-i18n="desktopBtn"
                        >
                            Confirm Booking Request
                            <svg
                                class="w-5 h-5"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                                ><path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg
                            >
                        </button>

                        <div id="mobile-checkout-summary" class="md:hidden sticky bottom-4 left-0 right-0 z-50 px-2 pointer-events-none">
                            <div class="bg-primary/95 backdrop-blur-md rounded-2xl p-3 shadow-2xl border border-white/20 flex items-center gap-3 pointer-events-auto">
                                <div class="min-w-[80px]">
                                    <p class="text-[9px] text-white/70 uppercase font-black tracking-wider leading-none mb-1">Total</p>
                                    <p class="text-lg font-black text-white leading-none whitespace-nowrap" id="mobile-summary-total">--</p>
                                </div>
                                <button
                                    id="mobile-pay-trigger"
                                    class="flex-1 bg-white text-primary font-black px-4 py-3 rounded-xl text-xs uppercase tracking-tighter transition-all active:scale-95 shadow-lg truncate"
                                    data-i18n="mobileBtn"
                                >
                                    REQUEST BOOKING
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Success Modal -->
    <div id="success-modal" class="fixed inset-0 z-[100] flex items-center justify-center px-6 hidden opacity-0 transition-opacity duration-500">
        <div class="absolute inset-0 bg-dark/80 backdrop-blur-sm"></div>
        <div class="relative bg-dark-soft border border-white/10 rounded-[32px] p-8 md:p-12 max-w-lg w-full shadow-2xl scale-95 transition-transform duration-500 overflow-hidden group">
            <!-- Animated Background Glow -->
            <div class="absolute -top-24 -right-24 w-48 h-48 bg-primary/20 blur-[80px] rounded-full"></div>
            <div class="absolute -bottom-24 -left-24 w-48 h-48 bg-green-500/10 blur-[80px] rounded-full"></div>

            <div class="text-center relative z-10">
                <!-- Premium Checkmark Animation -->
                <div class="mb-8 flex justify-center">
                    <div class="w-24 h-24 bg-green-500/10 rounded-full flex items-center justify-center border border-green-500/20 relative">
                        <div class="absolute inset-0 bg-green-500/20 blur-2xl rounded-full scale-0 group-hover:scale-100 transition-transform duration-1000"></div>
                        <svg class="w-12 h-12 text-green-500 success-checkmark" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" class="checkmark-path"></path>
                        </svg>
                    </div>
                </div>

                <h2 class="text-4xl font-black text-white mb-4 tracking-tighter uppercase" data-i18n="modalTitle">Request Received!</h2>
                <p class="text-gray-400 font-medium mb-8 leading-relaxed" id="modal-text" data-i18n="modalText">
                    Thank you! We will confirm your tour via WhatsApp. You can pay on arrival (cash or card) or ask us for a secure payment link on WhatsApp.
                </p>

                <div class="space-y-4">
                    <a href="https://wa.me/50687747250" target="_blank" rel="noopener" class="block w-full bg-green-500 text-white font-black py-4 rounded-2xl hover:bg-green-600 transition-all active:scale-95 text-sm uppercase tracking-widest shadow-xl">
                        Chat on WhatsApp
                    </a>
                    <a href="/" class="block w-full bg-white text-dark font-black py-4 rounded-2xl hover:bg-gray-100 transition-all active:scale-95 text-sm uppercase tracking-widest shadow-xl">
                        Return Home
                    </a>
                </div>
            </div>
        </div>
    </div>

    <style>
        .checkmark-path {
            stroke-dasharray: 100;
            stroke-dashoffset: 100;
            animation: dash 0.8s cubic-bezier(0.65, 0, 0.45, 1) forwards;
            animation-delay: 0.5s;
        }

        @keyframes dash {
            from { stroke-dashoffset: 100; }
            to { stroke-dashoffset: 0; }
        }

        .success-btn-vibe {
            background-color: #22c55e !important;
            box-shadow: 0 10px 30px -5px rgba(34, 197, 94, 0.4) !important;
        }
        
        #success-modal.show {
            display: flex !important;
            opacity: 1;
        }
        #success-modal.show > div:last-child {
            transform: scale(1);
        }
    </style>
</Layout>

<script>
    import { bookingStore } from "../store/booking";
    import { language } from "../store";

    // Form inputs (declared early so country selector can reference inputPhone)
    const inputPhone = document.getElementById("input-phone") as HTMLInputElement;

    // Country codes data
    const COUNTRIES = [
        { flag: "🇺🇸", code: "+1", name: "United States", dial: "1" },
        { flag: "🇨🇦", code: "+1", name: "Canada", dial: "1" },
        { flag: "🇨🇷", code: "+506", name: "Costa Rica", dial: "506" },
        { flag: "🇲🇽", code: "+52", name: "Mexico", dial: "52" },
        { flag: "🇬🇹", code: "+502", name: "Guatemala", dial: "502" },
        { flag: "🇸🇻", code: "+503", name: "El Salvador", dial: "503" },
        { flag: "🇭🇳", code: "+504", name: "Honduras", dial: "504" },
        { flag: "🇳🇮", code: "+505", name: "Nicaragua", dial: "505" },
        { flag: "🇵🇦", code: "+507", name: "Panama", dial: "507" },
        { flag: "🇨🇴", code: "+57", name: "Colombia", dial: "57" },
        { flag: "🇪🇨", code: "+593", name: "Ecuador", dial: "593" },
        { flag: "🇵🇪", code: "+51", name: "Peru", dial: "51" },
        { flag: "🇨🇱", code: "+56", name: "Chile", dial: "56" },
        { flag: "🇦🇷", code: "+54", name: "Argentina", dial: "54" },
        { flag: "🇧🇷", code: "+55", name: "Brazil", dial: "55" },
        { flag: "🇻🇪", code: "+58", name: "Venezuela", dial: "58" },
        { flag: "🇩🇴", code: "+1", name: "Dominican Republic", dial: "1809" },
        { flag: "🇵🇷", code: "+1", name: "Puerto Rico", dial: "1" },
        { flag: "🇨🇺", code: "+53", name: "Cuba", dial: "53" },
        { flag: "🇯🇲", code: "+1", name: "Jamaica", dial: "1" },
        { flag: "🇬🇧", code: "+44", name: "United Kingdom", dial: "44" },
        { flag: "🇩🇪", code: "+49", name: "Germany", dial: "49" },
        { flag: "🇫🇷", code: "+33", name: "France", dial: "33" },
        { flag: "🇪🇸", code: "+34", name: "Spain", dial: "34" },
        { flag: "🇮🇹", code: "+39", name: "Italy", dial: "39" },
        { flag: "🇳🇱", code: "+31", name: "Netherlands", dial: "31" },
        { flag: "🇧🇪", code: "+32", name: "Belgium", dial: "32" },
        { flag: "🇨🇭", code: "+41", name: "Switzerland", dial: "41" },
        { flag: "🇦🇹", code: "+43", name: "Austria", dial: "43" },
        { flag: "🇸🇪", code: "+46", name: "Sweden", dial: "46" },
        { flag: "🇳🇴", code: "+47", name: "Norway", dial: "47" },
        { flag: "🇩🇰", code: "+45", name: "Denmark", dial: "45" },
        { flag: "🇫🇮", code: "+358", name: "Finland", dial: "358" },
        { flag: "🇮🇪", code: "+353", name: "Ireland", dial: "353" },
        { flag: "🇵🇹", code: "+351", name: "Portugal", dial: "351" },
        { flag: "🇦🇺", code: "+61", name: "Australia", dial: "61" },
        { flag: "🇳🇿", code: "+64", name: "New Zealand", dial: "64" },
        { flag: "🇯🇵", code: "+81", name: "Japan", dial: "81" },
        { flag: "🇰🇷", code: "+82", name: "South Korea", dial: "82" },
        { flag: "🇨🇳", code: "+86", name: "China", dial: "86" },
        { flag: "🇮🇳", code: "+91", name: "India", dial: "91" },
        { flag: "🇷🇺", code: "+7", name: "Russia", dial: "7" },
        { flag: "🇮🇱", code: "+972", name: "Israel", dial: "972" },
        { flag: "🇦🇪", code: "+971", name: "UAE", dial: "971" },
        { flag: "🇿🇦", code: "+27", name: "South Africa", dial: "27" },
        { flag: "🇳🇬", code: "+234", name: "Nigeria", dial: "234" },
        { flag: "🇰🇪", code: "+254", name: "Kenya", dial: "254" },
        { flag: "🇪🇬", code: "+20", name: "Egypt", dial: "20" },
        { flag: "🇹🇷", code: "+90", name: "Turkey", dial: "90" },
        { flag: "🇵🇭", code: "+63", name: "Philippines", dial: "63" },
    ];

    // Country selector
    let selectedCountry = COUNTRIES[0]; // Default to US
    const countryBtn = document.getElementById("country-btn") as HTMLButtonElement;
    const countryDropdown = document.getElementById("country-dropdown") as HTMLDivElement;
    const countryList = document.getElementById("country-list") as HTMLDivElement;
    const countrySearch = document.getElementById("country-search") as HTMLInputElement;
    const countryFlag = document.getElementById("country-flag") as HTMLSpanElement;
    const countryCode = document.getElementById("country-code") as HTMLSpanElement;
    const fullPhoneInput = document.getElementById("input-full-phone") as HTMLInputElement;

    function renderCountries(filter: string = "") {
        const filtered = COUNTRIES.filter(c =>
            c.name.toLowerCase().includes(filter.toLowerCase()) ||
            c.code.includes(filter) ||
            c.dial.includes(filter)
        );
        countryList.innerHTML = filtered.map((c, i) => `
            <button type="button" data-idx="${COUNTRIES.indexOf(c)}" class="w-full flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-white/5 transition-colors text-left ${selectedCountry === c ? 'bg-primary/10' : ''}">
                <span class="text-lg">${c.flag}</span>
                <span class="text-white text-sm font-medium">${c.name}</span>
                <span class="text-gray-500 text-sm ml-auto">${c.code}</span>
            </button>
        `).join("");

        countryList.querySelectorAll("button").forEach(btn => {
            btn.addEventListener("click", () => {
                const idx = parseInt(btn.getAttribute("data-idx") || "0");
                selectedCountry = COUNTRIES[idx];
                countryFlag.textContent = selectedCountry.flag;
                countryCode.textContent = selectedCountry.code;
                countryDropdown.classList.add("hidden");
                updateFullPhone();
            });
        });
    }

    function updateFullPhone() {
        const raw = inputPhone.value.replace(/\D/g, "");
        fullPhoneInput.value = raw ? `${selectedCountry.dial}${raw}` : "";
    }

    countryBtn.addEventListener("click", (e) => {
        e.stopPropagation();
        countryDropdown.classList.toggle("hidden");
        if (!countryDropdown.classList.contains("hidden")) {
            countrySearch.value = "";
            renderCountries();
            countrySearch.focus();
        }
    });

    countrySearch.addEventListener("input", () => renderCountries(countrySearch.value));

    document.addEventListener("click", (e) => {
        if (!document.getElementById("country-selector")?.contains(e.target as Node)) {
            countryDropdown.classList.add("hidden");
        }
    });

    inputPhone.addEventListener("input", updateFullPhone);

    // Initial render
    renderCountries();

    // Selectors
    const summaryContainer = document.getElementById("order-summary-content");
    const totalEl = document.getElementById("summary-total");
    const subtotalEl = document.getElementById("summary-subtotal");
    const taxesEl = document.querySelector("#summary-total")?.parentElement?.previousElementSibling?.querySelector("span:last-child");
    const mobileTotalEl = document.getElementById("mobile-summary-total");
    const desktopPayBtn = document.getElementById("pay-button-desktop") as HTMLButtonElement;
    const mobilePayBtn = document.getElementById("mobile-pay-trigger") as HTMLButtonElement;

    // Bilingual UI strings (page SSR defaults to English)
    const I18N: Record<string, { en: string; es: string }> = {
        h1Title: { en: "Request Your Tour", es: "Solicita tu Tour" },
        requestSubtitle: {
            en: "No payment required today. Reserve your spot, we confirm via WhatsApp, and you can pay on arrival.",
            es: "Sin pago requerido hoy. Reserva tu cupo, confirmamos por WhatsApp y puedes pagar al llegar.",
        },
        noPayNowTitle: { en: "No payment now", es: "Sin pago ahora" },
        payOnArrivalText: {
            en: "You can pay when you arrive at the tour (cash or card), or ask us on WhatsApp for a secure payment link.",
            es: "Puedes pagar cuando llegues al tour (efectivo o tarjeta), o pedirnos un link de pago seguro por WhatsApp.",
        },
        dueOnArrival: { en: "Total due on arrival (no online payment).", es: "Total a pagar al llegar (sin pago en línea)." },
        desktopBtn: { en: "Confirm Booking Request", es: "Confirmar Solicitud" },
        mobileBtn: { en: "REQUEST BOOKING", es: "SOLICITAR RESERVA" },
        modalTitle: { en: "Request Received!", es: "¡Solicitud Recibida!" },
        modalText: {
            en: "Thank you! We will confirm your tour via WhatsApp. You can pay on arrival (cash or card) or ask us for a secure payment link on WhatsApp.",
            es: "¡Gracias! Confirmaremos tu tour por WhatsApp. Puedes pagar al llegar (efectivo o tarjeta) o pedirnos un link de pago seguro por WhatsApp.",
        },
    };

    function applyTranslations() {
        const lang = language.get();
        document.querySelectorAll("[data-i18n]").forEach(el => {
            const key = el.getAttribute("data-i18n");
            const entry = key && I18N[key];
            if (entry) el.textContent = lang === "es" ? entry.es : entry.en;
        });
    }

    language.subscribe(() => applyTranslations());
    applyTranslations();

    // Form inputs
    const inputFirstName = document.getElementById("input-firstName") as HTMLInputElement;
    const inputLastName = document.getElementById("input-lastName") as HTMLInputElement;
    const inputEmail = document.getElementById("input-email") as HTMLInputElement;
    const errorFirstName = document.getElementById("error-firstName");
    const errorLastName = document.getElementById("error-lastName");
    const errorEmail = document.getElementById("error-email");
    const errorPhone = document.getElementById("error-phone");

    const formFields = [
        { input: inputFirstName, error: errorFirstName },
        { input: inputLastName, error: errorLastName },
        { input: inputEmail, error: errorEmail },
        { input: inputPhone, error: errorPhone },
    ];

    // Clear error state on input
    formFields.forEach(({ input, error }) => {
        input?.addEventListener("input", () => {
            input.classList.remove("border-primary", "bg-primary/5");
            input.classList.add("border-white/10");
            error?.classList.add("hidden");
        });
    });

    // Validate form and highlight empty fields
    const validateForm = (): boolean => {
        let isValid = true;

        formFields.forEach(({ input, error }) => {
            if (!input) return;
            const value = input.value.trim();
            // Email validation
            const isEmailField = input.type === "email";
            const isEmpty = !value;
            const isInvalidEmail = isEmailField && value && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);

            if (isEmpty || isInvalidEmail) {
                input.classList.add("border-primary", "bg-primary/5");
                input.classList.remove("border-white/10");
                error?.classList.remove("hidden");
                isValid = false;
            } else {
                input.classList.remove("border-primary", "bg-primary/5");
                input.classList.add("border-white/10");
                error?.classList.add("hidden");
            }
        });

        if (!isValid) {
            // Scroll to first error
            const firstError = document.querySelector(".border-primary");
            firstError?.scrollIntoView({ behavior: "smooth", block: "center" });
        }

        return isValid;
    };

    // Helper for updating pay buttons state
    const setPayButtonsLoading = (isLoading: boolean) => {
        const lang = language.get();
        const idleText = lang === 'es' ? 'Confirmar Solicitud' : 'Confirm Booking Request';
        const idleTextMobile = lang === 'es' ? 'SOLICITAR RESERVA' : 'REQUEST BOOKING';
        const btns: Array<HTMLButtonElement | null> = [desktopPayBtn, mobilePayBtn];
        btns.forEach((btn, i) => {
            if (btn) {
                btn.disabled = isLoading;
                btn.innerText = isLoading
                    ? (lang === 'es' ? 'Enviando...' : 'Processing...')
                    : (i === 0 ? idleText : idleTextMobile);
            }
        });
    };

    // Vehicle tour detection — works with both slugs and titles
    function isVehicleTourSlug(slugOrTitle: string | null): boolean {
        if (!slugOrTitle) return false;
        const v = slugOrTitle.toLowerCase();
        return v.includes('side-by-side') || v.includes('side by side') || v.includes('jet-ski') || v.includes('jet ski') || v.includes('atv');
    }

    function vehicleName(slugOrTitle: string | null): string {
        if (!slugOrTitle) return 'Vehicles';
        const v = slugOrTitle.toLowerCase();
        if (v.includes('side-by-side') || v.includes('buggy')) return 'Buggies';
        if (v.includes('jet-ski')) return 'Jet Skis';
        if (v.includes('atv')) return 'ATVs';
        return 'Vehicles';
    }

    // Recalculate total if cart is stale (e.g. missing tourSlug from old localStorage)
    function recalculateTotal(state: Record<string, any>) {
        let total = 0;
        const vehicle = isVehicleTourSlug(state.tourSlug || state.tourTitle);
        if (vehicle) {
            const capacity = state.tourSlug === 'side-by-side-tour' || (!state.tourSlug && state.tourTitle?.toLowerCase().includes('side-by-side')) ? 4
                : state.tourSlug === 'jet-ski-tour' || (!state.tourSlug && state.tourTitle?.toLowerCase().includes('jet-ski')) ? 2
                : 1;
            const vehicles = Math.max(1, Math.ceil(state.adults / capacity));
            total = (vehicles * state.pricePerAdult) + (state.extraPassengers * 20);
        } else {
            total = (state.adults * state.pricePerAdult) + (state.children * state.pricePerChild);
        }
        const iva = total * 0.13;
        const finalTotal = total + iva;
        return { subtotal: total, iva, totalPrice: finalTotal };
    }

    // Subscribe to store
    bookingStore.subscribe((state) => {
        if (summaryContainer && state.tourId) {
            const vehicle = isVehicleTourSlug(state.tourSlug || state.tourTitle);
            const price = recalculateTotal(state);
            
            let guestsHtml = `${state.adults} Adults, ${state.children} Kids`;
            if (vehicle) {
                guestsHtml = `${state.adults} ${vehicleName(state.tourSlug || state.tourTitle)}, ${state.extraPassengers || 0} Extra Pax`;
            }

            // Update Main Summary List
            summaryContainer.innerHTML = `
                    <div class="flex gap-3 mb-4">
                        <div class="flex-1">
                            <h4 class="text-white font-bold leading-tight mb-1">${state.tourTitle || "Selected Tour"}</h4>
                            <p class="text-xs text-gray-400">Date: ${state.date ? new Date(state.date).toLocaleDateString() : "Not selected"}</p>
                            ${state.duration ? `<p class="text-xs text-primary font-medium mt-0.5">Duration: ${state.duration}</p>` : ''}
                        </div>
                    </div>
                <div class="flex justify-between text-sm mb-4">
                    <span class="text-gray-300">Selection</span>
                    <span class="text-white font-medium">${guestsHtml}</span>
                </div>
            `;
            
            // Update Prices
            if (subtotalEl) subtotalEl.textContent = `$${price.subtotal.toFixed(2)}`;
            if (taxesEl) taxesEl.textContent = `$${price.iva.toFixed(2)}`;
            if (totalEl) totalEl.textContent = `$${price.totalPrice.toFixed(2)}`;
            if (mobileTotalEl) mobileTotalEl.textContent = `$${price.totalPrice.toFixed(2)}`;

        } else if (summaryContainer) {
            summaryContainer.innerHTML = `<p class="text-gray-500 text-sm">No tour selected.</p>`;
        }
    });

    // Payment Logic — now creates a booking request without online payment
    const successModal = document.getElementById("success-modal") as HTMLElement;
    const modalTextEl = document.getElementById("modal-text") as HTMLElement;

    function showSuccessModal() {
        applyTranslations();
        successModal.classList.add("show");
    }

    const handlePayment = async () => {
        const state = bookingStore.get();
        const currentLang = language.get();
        if (!state.tourId) {
            alert(currentLang === 'es' ? 'Por favor selecciona un tour primero.' : "Please select a tour first.");
            return;
        }

        if (!validateForm()) return;

        try {
            setPayButtonsLoading(true);

            const formData = {
                firstName: inputFirstName.value.trim(),
                lastName: inputLastName.value.trim(),
                email: inputEmail.value.trim(),
                phone: fullPhoneInput.value || inputPhone.value.trim(),
            };

            // Recalculate price on the client to match server-side logic
            const price = recalculateTotal(state);

            // Creates the pending booking request (no payment / no redirect)
            const res = await fetch("/api/bookings/initiate", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                    customerName: `${formData.firstName} ${formData.lastName}`,
                    customerEmail: formData.email,
                    customerPhone: formData.phone,
                    tourId: state.tourId,
                    tourName: state.tourTitle,
                    tourSlug: state.tourSlug,
                    date: state.date,
                    time: state.time,
                    totalAmount: price.totalPrice,
                    adults: state.adults,
                    children: state.children,
                    extraPassengers: state.extraPassengers || 0,
                    language: currentLang,
                    pricePerAdult: state.pricePerAdult,
                    variationId: state.variationId,
                    duration: state.duration
                }),
            });

            const data = await res.json();

            if (data.success) {
                showSuccessModal();
            } else {
                alert(data.message || (currentLang === 'es' ? 'No se pudo enviar la solicitud.' : "Couldn't submit your request."));
            }
        } catch (e) {
            console.error(e);
            alert(currentLang === 'es' ? 'Ocurrió un error. Inténtalo de nuevo.' : "An error occurred. Please try again.");
        } finally {
            setPayButtonsLoading(false);
        }
    };

    // Close modal on backdrop click
    successModal.querySelector('.absolute.inset-0')?.addEventListener('click', () => {
        successModal.classList.remove('show');
    });

    // Check for errors in URL on load
    window.addEventListener('DOMContentLoaded', () => {
        const urlParams = new URLSearchParams(window.location.search);
        const error = urlParams.get('error');
        const debug = urlParams.get('debug');
        if (error) {
            let msg = "Payment issue: " + error;
            if (debug) {
                msg += "\n\nDebug Info: " + decodeURIComponent(debug);
            }
            alert(msg);
        }
    });

    [desktopPayBtn, mobilePayBtn].forEach(btn => {
        if (btn) btn.addEventListener("click", handlePayment);
    });
</script>
